# Fail, rather than warn, when the active Node version does not satisfy "engines".
# npm only reads the .npmrc in the package directory it runs from, so this file
# is duplicated in client/ and server/ until a root workspace exists.
engine-strict=true
